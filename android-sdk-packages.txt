# Android SDK packages for the Meshtastic workspace.
# Installed by: nix run .#bootstrap-sdk
#
# Coordinates use android-cli's slash form (platforms/android-37.0), NOT
# sdkmanager's semicolon form (platforms;android-37.0). If you fall back
# to raw sdkmanager, swap the separator.
#
# This file is the pin. These are exact versions and Google does not
# republish a released coordinate, so it gives reproducibility-in-practice
# without moving the SDK into /nix/store — which is what triggers the
# read-only-AGP / aapt2 mess.
#
# It is NOT hash-pinned. That is the honest gap versus androidenv:
# nothing here verifies the bytes Google serves.

# --- required by Meshtastic-Android + meshtastic-sdk (compileSdk 37) ---
platforms/android-37.0
build-tools/37.0.0
platform-tools

# Your installed cmdline-tools is 20.0.0 and only understands SDK XML v3,
# so it warns against v4 repos. 22.0.0 is available; this pulls it.
cmdline-tools/latest

# --- sources, for stepping into framework code from the IDE ---
sources/android-37.0

# --- emulator, for meshtastic-mcp's hardware-free e2e runs ---
emulator
system-images/android-36/google_apis_playstore/x86_64
